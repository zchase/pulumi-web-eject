export async function init(_stackName: string) {
    // Check it is a next.js project.

    // Find if there are an environment files
    // and transform to be used with panjs.

    // Create a new stack.

    // Sync the config.
}
